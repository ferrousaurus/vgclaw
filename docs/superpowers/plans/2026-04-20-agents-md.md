# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a root `AGENTS.md` that gives in-repo coding agents concise operational guidance for working on this VGC skills repository.

**Architecture:** Keep the change to one new root markdown file plus this planning artifact. The file should complement `README.md` by focusing on contributor workflow, repo structure, skill-authoring constraints, verification habits, and VGC-specific guardrails rather than end-user installation or usage.

**Tech Stack:** Markdown, git repository conventions, existing repo documentation patterns

---

### Task 1: Inspect current documentation patterns

**Files:**
- Read: `README.md`
- Read: `docs/superpowers/specs/2026-04-20-agents-md-design.md`
- Create: `AGENTS.md`

- [ ] **Step 1: Read the user-facing README for tone and overlap boundaries**

Read: `README.md`

Expected notes to carry forward:

```text
- Repository is skill-focused and markdown-heavy.
- README is user-facing: installation, choosing skills, and domain explanations.
- AGENTS.md must avoid restating installation instructions or user workflow verbatim.
```

- [ ] **Step 2: Read the approved design spec and extract required sections**

Read: `docs/superpowers/specs/2026-04-20-agents-md-design.md`

Expected section checklist:

```text
- Repo Overview
- Important Paths
- Working Conventions
- Skill Authoring Notes
- Verification Checklist
- VGC Domain Notes
- Change Boundaries
```

- [ ] **Step 3: Verify the root file does not already exist**

Run: `ls "AGENTS.md"`
Expected: shell reports the file does not exist yet

### Task 2: Write the new root guide

**Files:**
- Create: `AGENTS.md`

- [ ] **Step 1: Write the complete root AGENTS guide**

Create `AGENTS.md` with this content:

```markdown
# AGENTS.md

## Repo Overview

This repository contains agent skills and supporting reference material for Pokemon Champions VGC team building and evaluation workflows. Most changes in this repo are markdown or data-file edits rather than application code changes.

Use `README.md` for user-facing installation and skill selection context. Use this file for in-repo contributor guidance.

## Important Paths

- `.agents/skills/`: skill definitions, bundled resources, and skill-local references
- `docs/superpowers/specs/`: design specs for recent work
- `docs/superpowers/plans/`: implementation plans for approved work
- `README.md`: user-facing overview and installation instructions

When editing a skill, inspect nearby files first so your changes match the existing structure and wording style.

## Working Conventions

- Prefer the smallest correct change.
- Preserve existing prompt structure and tone unless the task is explicitly rewriting them.
- Keep instructions concrete. Avoid vague guidance that leaves room for agent guesswork.
- Do not duplicate guidance already maintained in another file unless the duplication is intentional and necessary.
- If behavior changes, update the closest relevant documentation in the same change.

## Skill Authoring Notes

- Follow the existing organization pattern in each skill directory.
- Keep reasoning and workflow guidance in markdown files.
- Keep structured facts and machine-readable values in data files.
- If a skill references a file, command, or dependency, verify that it exists and that the name matches exactly.
- Avoid adding speculative workflow steps, unsupported tool assumptions, or fake capabilities.

## Verification Checklist

Before finishing a change:

- Read adjacent files to confirm local conventions.
- Verify any referenced paths, file names, and links.
- Check that updated instructions still match the actual repository layout.
- If you changed structured data, do a consistency pass for field names and value formats.
- Run relevant validation or tests when the task or repo provides them.

## VGC Domain Notes

- Showdown paste format is the default team interchange format.
- Teams are built for Level 50 play.
- Total EVs are capped at 508.
- Teams cannot contain duplicate Pokemon or duplicate held items.
- Do not invent current meta claims, usage stats, or matchup data without a real source.

## Change Boundaries

- Do not rewrite large prompt sections when a local edit is enough.
- Do not add new files or workflows unless the task actually requires them.
- Do not turn `README.md` into contributor guidance; keep README user-facing and `AGENTS.md` contributor-facing.
```

- [ ] **Step 2: Review the file for overlap and trim any README duplication**

Review target:

```text
- No install commands
- No skill selection walkthroughs
- No repeated long-form product overview from README
```

- [ ] **Step 3: Save only the minimal content needed for contributor guidance**

Expected result:

```text
AGENTS.md is concise, operational, and scoped to repository contributors and coding agents.
```

### Task 3: Verify the new guide matches the repo

**Files:**
- Verify: `AGENTS.md`
- Verify: `README.md`

- [ ] **Step 1: Re-read the finished AGENTS file for internal consistency**

Read: `AGENTS.md`

Checklist:

```text
- Section names match the approved design.
- No placeholders or TODO language remain.
- All paths referenced in the file exist.
```

- [ ] **Step 2: Verify the referenced directories exist in the repository**

Run: `ls ".agents/skills" && ls "docs/superpowers/specs" && ls "docs/superpowers/plans"`
Expected: each directory lists successfully

- [ ] **Step 3: Check git diff to confirm the change is scoped correctly**

Run: `git diff -- AGENTS.md docs/superpowers/specs/2026-04-20-agents-md-design.md docs/superpowers/plans/2026-04-20-agents-md.md`
Expected: diff shows one new root guide plus the design and plan docs only
